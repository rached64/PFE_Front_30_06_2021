/*import { ICategories } from "./categories";

export interface IPagination {
    pageIndex: number;
    pageSize: number;
    count: number;
    data: ICategories[];
  }
export class PaginationCategorie implements IPagination {
    pageIndex: number;
    pageSize: number;
    count: number;
    data: ICategories[] = [];
}

*/